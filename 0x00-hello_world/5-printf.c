#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char my_string[] = "with proper grammar, but the outcome is a piece of art,\n";
printf("%s", my_string);
return (0);
}
