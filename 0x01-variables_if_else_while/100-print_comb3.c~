#include <stdio.h>
/**
 * main - Entry point:Prints numbers 00 to 99
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m;
	int n;

	for (m = '0'; m <= '9'; m++)
	{
		for (n = '0'; n <= '9'; n++)
		{
			putchar(m);
			putchar(n);
			if (n < '9' && m < '9')
			{
				putchar(',');
				putchar(' ');
			}
		}
	}
	putchar('\n');
	return (0);
}
